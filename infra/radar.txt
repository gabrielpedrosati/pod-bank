14.07.2023

# Geral
    - validar nome do serviços
    - Padrão de nome dos serviços ({produto}-{ambiente}-{camada})
    - Inserir um data quality
    - Definir default_tags

# Data quality
    - Será iniciado via Lambda assim que um novo arquivo cair na camada Raw.
    - Salvar os dados na trusted em Parquet - compactação Snappy.
    - Ler arquivo de configuração
    - Ler arquivos no bucket
    - Validar arquivo na configuração
    - Validar expectativas 

# S3
    - Versionamento de arquivos (importante para Trusted e Refined)
    - Deixar privados
    - Criar buckets awsresources (irá armazenar tudo que os serviços precisam armazenar, exemplo: query Athena)
    - Criar bucket resources (arquivos de configuração, scripts do emr...)

# IAM 
    - criar policy para grupo de BI
    - validar serviços utilizados pela equipe de engenharia de dados (emr, glue, eventbridge, stepfunctions, athena, s3)

# Glue Tables
    - definir schedule dos crawlers;
    - definir tabelas que armazenarão as variáveis;

# EMR
    - Como iremos lidar com o armazenamento de novos arquivos (na verificação de arquivos já existentes com o mesmo nome...)
    - Verificar possibilidade de utilizar lambda para startar um cluster EMR e adicionar os steps dentro do cluster.

# Lambda
    - Cobrar ao gestor técnico as credenciais para a conta do repositório S3 da PoD Bank.